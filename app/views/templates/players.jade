each player in players
    .player
        if player.theirTurn
            span.their-turn >>
        if player.you
            span.you [you]
        span.name= player.name
        if player.vachettes === 1
            span (1 cow eaten)
        else if player.vachettes > 1
            span (#{player.vachettes} cows eaten)
